/**
 * email/search.ts -- Gmail search stub (no-op in open-source version).
 */

export async function searchEmails(_plan: any): Promise<any[]> {
  console.log('[mittens-open] Email search not available');
  return [];
}
